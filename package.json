{
  "name": "my-app-test",
  "version": "0.1.0",
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "scripts": {
    "version": "echo $npm_package_version $npm_package_name",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --coverage --env=jsdom",
    "eject": "react-scripts eject"
  },
  "publishConfig": {
    "registry": "https://dev1.mesos.rccl.com/nexus/repository/npm-private/"
  }
}
